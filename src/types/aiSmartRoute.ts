
// Re-export types from the main types file for backward compatibility
export type { 
  Collaborator,
  TripCoordinate,
  SavedPlace,
  Trip,
  OptimizedPlace,
  DayItinerary,
  RouteConfiguration
} from '@/types';
